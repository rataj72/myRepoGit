@using Microsoft.Ajax.Utilities
@model WebApp.Models.DBclass

<h3>hello db</h3>

@using (Html.BeginForm("SaveIT", "DB", FormMethod.Post))
{
    <br/>
    <div>potwierdzenie zapisu:   @Model.number</div>
    <br />
    <div>
        @Html.TextBoxFor(model => model.number , new {pattern ="[0-9]{2,}" , placeholder = Model.number})

        <br />
        <div>
            <br />
            @Html.DropDownListFor(item => item.DropList, Model.DropList)
        </div>
   
    </div>
    <br /><br />
    <input type="submit" name="but" value="do bazy" />

    
}
